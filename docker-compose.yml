version: '2'
services:
  web:
    image: alexcheng/magento:1.6.2.0
    ports:
      - "80:80"
    links:
      - mysql
    env_file:
      - env
  mysql:
    image: mysql:5.6
    env_file:
      - env
